FROM media-stream-analyzer:latest

# Dev tools
RUN pip3 install --no-cache-dir -r requirements-dev.txt

# Mount source for live reload
VOLUME ["/app/src"]

CMD ["uvicorn", "src.api.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
